import { VideoTimeagoPipe } from './video-timeago.pipe';

describe('VideoTimeagoPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoTimeagoPipe();
    expect(pipe).toBeTruthy();
  });
});
